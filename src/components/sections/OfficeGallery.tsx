// Kept as a compatibility export for older page imports. No former-office imagery.
import { Studio } from "./Studio";
export function OfficeGallery(_props: { showCTA?: boolean; title?: string; subtitle?: string }) {
  return <Studio />;
}
